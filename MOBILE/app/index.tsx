/**
 * Index Screen - Entry Point (Start Page)
 *
 * Landing page similar to CLIENT's StartPage
 * Users can explore or login
 */

import StartPage from './start';

export default function IndexScreen() {
  return <StartPage />;
}

